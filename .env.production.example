# Copy to .env.production for docker compose -f docker-compose.prod.yml

DATABASE_URL=postgresql://postgres:postgres@postgres:5432/honeypush

# Public URL shown in metadata (your domain or http://server-ip:3000)
# NEXT_PUBLIC_SITE_URL=http://YOUR_SERVER_IP:3000

# LLM (optional — rule-based fallback if empty)
# LLM_API_KEY=
# LLM_BASE_URL=https://api.deepseek.com/v1
# LLM_MODEL=deepseek-chat

# Cron digest (optional)
# CRON_SECRET=
